import { StyleSheet } from 'react-native';

const headerStyles = StyleSheet.create({
  borderlessHeader: {
    borderBottomWidth: 0
  },
  title: {
    fontSize: 17,
    fontWeight: '600',
    fontFamily: 'System'
  }
});

export default headerStyles;
